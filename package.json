{
  "name": "bos-workspace-docs",
  "version": "0.0.0",
  "repository": "https://github.com/NEARBuilders/bos-workspace-docs.git",
  "author": "nearbuilders",
  "license": "MIT",
  "scripts": {
    "fmt": "prettier --write '**/*.{js,jsx,ts,tsx,json}'",
    "fmt:check": "prettier --check '**/*.{js,jsx,ts,tsx,json}'",
    "bw": "bos-workspace",
    "dev": "bw dev",
    "dev:testnet": "bw dev --network testnet",
    "build": "bw ws build",
    "build:testnet": "bw ws build -n testnet"
  },
  "devDependencies": {
    "bos-workspace": "^1.0.0-alpha",
    "prettier": "^2.8.8"
  }
}
